# Memory.md

> **預設空白** — 跨對話記憶由官方 Auto Memory 管理（`autoMemoryEnabled: true`，見 `/memory` 指令）。
> 此檔僅作相容性佔位，不進入 CLAUDE.md @import 鏈，不計入啟動成本。

---

## 近期關鍵決策

- **導入 The Loop Harness 大改版**：將 `.claude/rules/` 重建為 The Loop 六階段行為契約（OBSERVE → IDENTIFY → PROPOSE → APPLY → TEST → RECORD），作為 workspace 唯一行為準則。同步精簡 docs 與整理 scripts。
